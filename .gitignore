# Build output (generated by CI, never committed)
dist/
_site/

# Node
node_modules/

# Jekyll leftovers
.sass-cache
.jekyll-cache
.jekyll-metadata

# Editor / OS
.nova
.DS_Store

# Ruby (legacy)
vendor
.bundle

# Secrets / local
token
.env.local

# Metadata cache — CI restores/saves its own via actions/cache; local builds
# generate this and it shouldn't be committed.
data/llm-cache.json
